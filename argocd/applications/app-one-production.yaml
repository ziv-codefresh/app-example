apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-one-production
  namespace: argocd
  annotations:
    argo.octopus.com/project: "test2"
    argo.octopus.com/environment: "production"
    argo.octopus.com/tenant: "emea"
spec:
  project: default
  source:
    repoURL: https://github.com/ziv-codefresh/app-example
    targetRevision: main
    path: apps/app-one/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
